const main = () => {
//Festival de música 
/*const yearborn = (age) => {
    for (i=0; i<=2004; i++);
}
const isApproved = (yearborn) => {
    if(yearborn<=2004){
        console.log ("Es mayor de edad, puede pasar, bienvenido");
    }else {
        console.log("No es mayor de edad, puede pasar, a retirarse :v");
    }
}
isApproved(2004);*/

//Funcion que sea un discriminante para ecuaciones
 /*function suma(a,b,c){
        const result =b**2-4*a*c
        return result
    }
const discriminante= (suma) => {
        if(suma>0){
            console.log("Tiene dos soluciones");
        }
    
        if (suma<0){
            console.log("No tiene solucion");
        }   
        if (suma===0){
            console.log("Tiene una solucion");
        }
    }
//Los valores de la funcion se meten aquí abajo con el orden a,b,c
const res = suma(8,15,18);
console.log (`El resultado es ${res}`);
discriminante(res); */

//Array. push 
movies =[];
movies.push ("Sucker Punch");
movies.push ("Scott Pilgrim vs the world");
movies.push ("The Baudelaire");
movies.push ("The invisible man");
movies.push ("La niña de la capa roja");
movies.push ("Suicide Room");
movies.push ("Alice in wonderland 1 y 2 ");
movies.push ("Corpse Bride");
console.log(movies);
}
main();

